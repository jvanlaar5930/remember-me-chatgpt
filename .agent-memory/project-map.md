# Project Map: remember-me-chatgpt

Generated by: create-chat-gpt-repo-memory  
Template: generic

## Purpose

`create-chat-gpt-repo-memory` is an npm CLI scaffolding tool. It generates Git-tracked repo memory files (`AGENTS.md` + `.agent-memory/`) into any repository, so ChatGPT and Codex-style agents have durable project context without repeatedly scanning the whole codebase.

## Tech Stack

- Runtime: Node.js >=18 (ESM, `"type": "module"`)
- Package manager: npm
- CLI framework: [commander](https://www.npmjs.com/package/commander) ^12.1.0
- No database, no frontend, no build step — plain Node.js ESM source

## Repository Layout

```
bin/cli.js                  CLI entry point (commander setup, runInit)
src/init.js                 Core scaffold logic (file generation, AGENTS.md merge)
src/gitignore.js            .gitignore read/write/idempotent update logic
src/paths.js                resolveTargetDirectory, getPackageRoot
src/projectName.js          getProjectName (basename of rootDir)
src/templateResolver.js     SUPPORTED_TEMPLATES, validateTemplateName, applyTemplateVariables
templates/generic/          Generic template files (8 files)
templates/dotnet/           .NET-specific template files (8 files)
.agent-memory/              This project's own repo memory (meta)
README.md                   Full usage, options, manual test plan, publishing notes
package.json                name: create-chat-gpt-repo-memory, version: 0.1.0
```

## Entry Points

- `bin/cli.js` — CLI entry point; invoked as `create-chat-gpt-repo-memory` or `npx create-chat-gpt-repo-memory`
- `src/init.js` exports `initChatGptRepoMemory(options)` — called by CLI

## Major Domains

### CLI / Command Parsing (`bin/cli.js`)
- Defines options: `--force`, `--no-gitignore`, `--template <name>`, `--cwd <path>`
- Resolves target directory, calls `initChatGptRepoMemory`, prints results

### Scaffold Engine (`src/init.js`)
- Generates 8 files per template into target directory
- AGENTS.md merge logic: appends managed block if no existing repo-memory guidance, updates existing managed block in-place, or prompts user if conflict cannot be resolved automatically
- Managed block delimited by HTML comments: `<!-- create-chat-gpt-repo-memory:start -->` / `<!-- create-chat-gpt-repo-memory:end -->`

### Template System (`src/templateResolver.js`, `templates/`)
- Two templates: `generic`, `dotnet`
- Template variables: `{{PROJECT_NAME}}`, `{{TEMPLATE_NAME}}`, `{{GENERATED_BY}}`
- `applyTemplateVariables` throws if any `{{...}}` placeholders remain unreplaced

### .gitignore Management (`src/gitignore.js`)
- Adds `.agent-memory-local/` and local DB/log patterns
- Idempotent: does not duplicate block if marker already present
- Removes legacy `.chatgpt-local/` and `.chatgpt/**/*` patterns when updating

## Critical Files

- `bin/cli.js` — CLI entry, options, output formatting
- `src/init.js` — all scaffold and merge logic lives here
- `templates/generic/` and `templates/dotnet/` — the actual content shipped to users
- `package.json` — `"bin"` field, `"files"` list, `"engines"`, single dependency

## Configuration and Secrets

- No configuration files (no `.env`, no config schema)
- No secrets involved at any layer
- `--cwd` and `--template` are the only runtime inputs

## Data and State

- No database, no persistent state
- Filesystem is the only stateful system (reads/writes files in the target directory)
- All operations are idempotent by design

## Testing and Validation

- No automated test suite (no test runner, no test files found)
- Manual test plan documented in README.md (covers create, re-run, --force, --template dotnet, invalid template, stale-term grep)
- Validation: `node bin/cli.js` run against a temp directory

## Build, Run, and Deployment

- No build step; source is run directly
- Local dev: `node bin/cli.js` or `npm link` then `create-chat-gpt-repo-memory`
- Publish: `npm publish --access public` (requires npm 2FA OTP or granular access token with bypass-2FA)
- Published to: https://registry.npmjs.org/ as `create-chat-gpt-repo-memory`

## Do Not Scan By Default

- `node_modules/`
- `.git/`

## Known Gotchas

- Package is ESM (`"type": "module"`); all imports must use ESM syntax and `.js` extensions
- `npm publish` requires 2FA: use `--otp=<code>` or a granular access token with bypass-2FA enabled
- Non-interactive environments (CI, piped stdin) fall back to "skip" when AGENTS.md merge cannot be resolved automatically
- `getPackageRoot()` derives the package root from `import.meta.url` in `paths.js` — this breaks if `paths.js` is moved relative to the package root
